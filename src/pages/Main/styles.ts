import { Box } from '@mui/material';
import { spacing } from '../../styles/accessors';
import styled from '../../styles/styled-components';

export const SCContainer = styled(Box)`
  padding: ${spacing(10)} ${spacing(9)} 0 ${spacing(9)};
`;
